{ ≈стественное (Ќеймановское) сли€ние. }

Repeat {ѕовторение актов сли€ний подсписков}
  If A[j].kl < A[i].kl Then {¬ыбираетс€ меньша€ запись}
    Begin
      sp[k] := j;
      k := j;
      j := sp[j];
      If j<=0 Then {—цепление с остатком "i"-подсписка}
        Begin
          sp[k] := i;
          Repeat
            m := i;
            i := sp[i]
          Until i<=0
        End
    End
  Else
    Begin
      sp[k] := i;
      k := i;
      i := sp[i];
      If i<=0 Then {—цепление с остатком "j"-подсписка}
        Begin
          sp[k] := j;
          Repeat
            m := j;
            j := sp[j]
          Until j<=0
        End
    End;
  If j<=0 Then
    Begin
      sp[m] := 0;
      sp[p] := -sp[p];
      i := -i;
      j := -j;
      If j<>0 Then p := r;
      k := r;
      r := m
    End
Until j=0;

{¬ конец сформированного подсписка всегда заноситс€ нулева€ ссылка (sp[m]:= 0),
ибо он может оказатьс€ последним.

ƒействие sp[p]:= -sp[p] обозначает минусом конец ранее построенного подсписка.


¬ переменных i,j ссылки на начала новых сливаемых подсписков - со знаком минус; его снимаем.
ѕереход к новым подспискам требует обновлени€ переменных p, k, r}
